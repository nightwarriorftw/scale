import React from 'react';

const Home = () => {
    return(
        <div style={{textAlign: 'center'}}>
            <h1>Scale - Host and Schedule Interviews</h1>
        </div>
    )
}

export default(Home);
